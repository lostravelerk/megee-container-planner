# 浙江美集集装箱装柜规划 v2.2 检验与验收标准

本标准是首版发布门槛。所有必检项通过后，才允许同步 GitHub 并发布 Cloudflare 线上版本。

## 1. 功能范围

- 最大包装单元必须可选：`纸箱` / `托盘`。
- 美集默认纸箱尺寸为 480 × 380 × 350 mm，默认托盘尺寸为 1000 × 1200 × 150 mm；恢复默认必须回到这两组数值。
- EA/BOX 不设默认值；空白时总 EA 显示待填写，输入正整数后总 EA = 纸箱总数 × EA/BOX，并同步到柜型对比和摘要。
- 产品数据以标准 `.xlsx` 导入；字段严格限定为家族、产品代码、品名、EA/BOX、外箱尺寸，不读取规格、成本、BOM、客户或其他业务字段。报告备注由本系统人工填写并只保存在当前浏览器。
- 首次打开可载入随版本发布的产品快照；用户导入 Excel 后以产品代码为唯一键在本机重建全量方案，不向服务器上传 Excel 或业务数据。
- 产品方案库必须支持按产品代码、品名、家族和报告备注检索；列表固定显示家族、产品代码、品名、EA/BOX、外箱尺寸、20GP、40GP、40HQ、备注和明细报告入口。
- EA/BOX 或外箱尺寸缺失的产品必须标记“待补充”，三种柜型装量均为 0，并禁用明细与报告，不能使用通用默认尺寸形成误导结论。
- 从方案库打开记录必须恢复当时的完整参数快照；点击客户报告必须打开该版本报告，不得用最新主数据静默覆盖历史结果。
- 纸箱模式：输入纸箱长、宽、高；箱高固定朝上，底面允许 90° 旋转与规则混排。
- 不允许纸箱侧放、倒置、挤压、重叠或越界；高度层数按有效净高向下取整，同层相邻纸箱保留设定间隙。
- 托盘模式：纸箱、托盘长宽高均可自定义；默认使用平底托盘并允许上下双层。算法必须同时比较单层高托与双层矮托，以全柜纸箱总数优先；数量相同时选择托盘数更少的单层方案。
- 托盘客户高度区间默认 1500–1800 mm，可按项目修改；双层时每个托盘仍须达到客户最低高度，且两托总叠高不得超过集装箱有效净高。上下托盘均只能使用完整纸箱层，不压箱、不斜放、不虚构补高。
- 托盘外廓占有效柜容的最低门槛默认 70%；低于门槛的自动批量规划必须放弃托盘并推荐纸箱直装，手动托盘分析视图可保留用于复核。
- 集装箱支持 20GP、40GP、40HQ 预设，且内部长宽高可覆盖为自定义值。
- 默认同步计算 20GP、40GP、40HQ/40HC 三种常用国际柜型，并可一键切换完整可视化。
- 纸箱尺寸余量、纸箱水平间隙、托盘尺寸余量、托盘间隙、托盘退边、箱门余量、左右余量和顶部余量必须可配置。

## 2. 计算正确性（必须全部满足）

- 所有尺寸和余量以 mm 计算；纸箱尺寸余量加入长、宽、高，间隙只加在相邻包装单元之间。
- 有效箱内空间 = 内长减箱门余量、内宽减两侧余量、内高减顶部余量，不得出现负数。
- 任一可视化矩形不得越过其有效承载面的边界；任意两个包装单元不得重叠。
- 纸箱直装数量 = 每层数量 × 高度层数。
- 托盘模式总箱数 = 柜底托位数 × 上下托盘层数 × 每托盘每层箱数 × 每托盘纸箱层数。
- 托盘上的纸箱不得越过退边后的有效托盘面；托盘总高不得超过用户限高或集装箱有效内高中的较小值。
- 托盘计费体积 = 实际托盘总数 × 含公差托盘长 × 含公差托盘宽 × 实际单托总高；纸箱直装计费体积 = 总箱数 × 纸箱标称外廓体积，统一换算为 CBM。
- 启用平底托盘双层后，两层实际总叠高不得超过集装箱有效净高；若双层未增加纸箱总数，必须选择托盘更少的单层方案。
- 算法必须比较长宽互换，并搜索两个主轴方向的混合分带方案；选取数量较高者。
- 任一输入变化必须立即重新计算并只呈现规则约束内的最优方案；先最大化包装单元数量，数量相同再最小化组合余隙。
- 纸箱体积利用率使用纸箱标称体积计算，且应与总箱数一致。
- 输入无效、空间不足或尺寸超限时数量为 0，并给出明确提示，不得出现 `NaN`、负数量或页面崩溃。

## 3. 可视化一致性

- 必须提供俯视水平剖面、侧视纵向剖面和箱门端视横向剖面；托盘模式另提供单托盘排箱视图。
- 俯视图中正向、旋转 90°、预留空隙必须使用不同且有图例说明的视觉编码。
- 俯视图中的纸箱/托盘数量（未触发展示上限时）必须等于当前层计算数量。
- 侧视图层数、已用高度、剩余高度必须与摘要数字一致。
- 托盘模式必须同时标注柜底托位数、上下托盘层数、实际托盘总数、每托纸箱层数、单托总高、两层总叠高和柜内剩余高度。
- 托盘模式必须提供独立的“单托盘排箱”视图，并显示每层、层数、每托总箱数。
- 图面必须标注集装箱长度、宽度或高度，且在窄屏允许横向查看，不能压缩到不可读。

## 4. 交互与界面

- SAP 风浅色紧凑界面；表单、表格、统计和主操作具有清晰层级，使用专业蓝灰配色、明确边界和高信息密度。
- 桌面端和手机端均可完成所有输入、模式切换和视图切换；键盘可聚焦所有表单与按钮。
- 手机端宽度 320–430 px 时不得出现页面级横向溢出；输入和主按钮触控高度不小于 44 px，剖面图在局部容器内横向滑动查看。
- 软件页面底部必须显示浙江美集实业有限公司、版权年份、MEGEE COSPACK 和软件版本号；正式报告页脚必须显示相同版本号和报告编号，便于追溯。
- 修改任一输入后，统计数字、摘要和可视化必须同步更新。
- “恢复默认”还原默认尺寸和标准余量；“打印方案”可生成无控制面板干扰的打印版。
- “输出报告”必须生成正式打印版，包含报告编号/日期、全部参数、EA、三柜型对比、最优方案、多剖面图、摆放原则、风险说明和签字栏，并可由浏览器存为 PDF。
- 正式报告必须可在“中文版”和“English”之间选择；英文版必须完整翻译字段、操作步骤、图名、图内方向标注、复核原则和签字栏，不能只翻译标题。
- 正式报告必须包含外箱 L × W × H 尺寸示意图，明确箱高向上和底面仅可旋转 90°；不得只用一行尺寸文字替代现场可识别图示。
- 正式报告必须把算法结果转为操作指令：从箱头向箱门的装载方向、图面编号顺序、每层 0°/90° 数量、总层数，以及托盘模式的每托每层数量、纸箱层数和上下托盘层数。
- 正式报告的俯视、纵向剖面、箱门横向剖面必须与装量结论一致；托盘模式另附单托排箱图。操作人员仅凭报告即可识别产品、包装尺寸、选用柜型、装箱数量和具体摆放方式。
- 托盘方案未达到客户高度或最低柜容利用率时，报告必须显著标注“不推荐执行”，不得把分析方案表述为正式执行方案。
- 报告跨页表格允许自然续页，续页必须重复表头；章节标题、图示和签字栏不得孤立、裁切或重叠。
- 正式报告必须显示包装总材积 CBM。
- 装柜算法和 Excel 解析只在浏览器内完成；导入数据仅写入当前浏览器本地存储。

## 5. 工程与发布

- 生产构建无 TypeScript/打包错误。
- 自动化算法测试全部通过，至少覆盖：混合方向优于单一方向、间隙改变数量、越界/重叠检查、超大包装单元返回 0、标准托盘组合、40HQ 双层增加数量、20GP/40GP 数量相同时回退单层、禁用双层。
- GitHub 仓库源码与 Cloudflare 发布所用提交必须一致。
- 本地地址、Cloudflare 线上地址均能正常打开并完成两种模式计算。
- 正式线上地址必须位于 Cloudflare Workers、自有域名或 Pages，不得使用 `chatgpt.site`。
- 公开部署不得包含 Cost 凭据、成本、BOM、客户或其他保密字段；随版本提供的产品快照只能包含本标准允许的五个字段。
- Excel 导入必须幂等、可追踪；失败时保留最后一次成功导入的浏览器缓存并显示错误，不得把失败显示为成功。

## 6. 产品导入与报告治理

- 产品代码是导入唯一键；同一 Excel 内重复产品代码必须拒绝导入，重复导入不得生成重复 SKU。
- EA/BOX 允许为空，不得默认填充；包装尺寸缺失时记录必须标记“待补齐”。
- 每次导入必须重新计算全量自动方案，同时保留相同产品代码的人工备注与复核状态。
- 报告必须包含产品数据导入时间、装柜算法版本、方案版本、生成日期和复核状态。
- 客户可使用公开软件自行导入标准模板；公开页面不得自动连接内部 Cost 系统。

## 7. 明确边界

首版输出为规则化工程预估，不替代现场复核。当前不计算轴载、总载重、纸箱抗压、装卸顺序、门框/角柱局部障碍和复杂自由形状拼箱；页面必须持续显示此限制。
