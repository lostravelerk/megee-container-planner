# 浙江美集集装箱装柜规划 v2.5 检验与验收标准

本标准是首版发布门槛。所有必检项通过后，才允许同步 GitHub 并发布 Cloudflare 线上版本。

## 1. 功能范围

- 最大包装单元必须可选：`纸箱` / `托盘`。
- 美集默认纸箱尺寸为 480 × 380 × 350 mm，默认托盘尺寸为 1000 × 1200 × 150 mm；恢复默认必须回到这两组数值。
- EA/BOX 不设默认值；空白时总 EA 显示待填写，输入正整数后总 EA = 纸箱总数 × EA/BOX，并同步到柜型对比和摘要。
- 产品数据以标准 `.xlsx` 导入；输入字段严格限定为系列、产品代码、品名规格、产品数量、EA/BOX、外箱尺寸、包装方式，以及托盘包装时的托盘尺寸；向后兼容旧模板“家族”和“品名”表头。不读取成本、BOM、客户或其他业务字段。报告备注由本系统人工填写并只保存在当前浏览器。
- 公开部署首次打开必须是空方案库，不得随版本发布或自动获取企业产品快照；用户导入 Excel 后以产品代码为唯一键在本机重建全量方案，不向服务器上传 Excel 或业务数据，并可明确清空当前浏览器中的全部产品与方案版本。
- 产品方案库必须支持按产品代码、品名规格、系列和报告备注检索；列表固定显示系列、产品代码、品名规格、EA/BOX、外箱尺寸、20GP、40GP、40HQ、备注和明细报告入口；每个柜型必须同时显示纸箱数 BOX 与成品数量 EA。
- 输入方式必须包含两条完整路径：Excel 批量导入后校验、批量计算并进入方案库；手工单笔可编辑系列、产品代码、品名规格、产品数量、EA/BOX、外箱、包装方式及托盘尺寸，即时计算并可保存入库。
- 美集产品外箱尺寸为空时必须自动采用 480 × 380 × 350 mm，并在模板和导入结果中明确；SKU 提供实测专用尺寸时必须优先使用实测值。EA/BOX 缺失或托盘包装缺少托盘尺寸时必须标记“待补充”，三种柜型装量均为 0，并禁用明细与报告。
- 从方案库打开记录必须恢复当时的完整参数快照；点击客户报告必须打开该版本报告，不得用最新主数据静默覆盖历史结果。
- 纸箱模式：输入纸箱长、宽、高；箱高固定朝上，底面允许 90° 旋转与规则混排。
- 不允许纸箱侧放、倒置、挤压、重叠或越界；高度层数按有效净高向下取整，同层相邻纸箱保留设定间隙。
- 托盘模式：纸箱、托盘长宽高均可自定义；默认使用平底托盘并允许上下双层。算法必须同时比较单层高托与双层矮托，以全柜纸箱总数优先；数量相同时选择托盘数更少的单层方案。
- 托盘客户高度区间默认 1200–1800 mm，可按项目修改；默认接受平底托盘双层，双层时每个托盘仍须达到客户最低高度，且两托总叠高不得超过集装箱有效净高。上下托盘均只能使用完整纸箱层，不压箱、不斜放、不虚构补高。
- 托盘外廓占有效柜容的最低门槛默认 70%；低于门槛的自动批量规划必须放弃托盘并推荐纸箱直装，手动托盘分析视图可保留用于复核。
- 集装箱支持 20GP、40GP、40HQ 预设，且内部长宽高可覆盖为自定义值。
- 默认同步计算 20GP、40GP、40HQ 三种常用国际柜型，并可一键切换完整可视化；40HC 同义缩写统一显示为 40HQ，不作为第四种柜型。
- 纸箱尺寸余量、纸箱水平间隙、托盘尺寸余量、托盘间隙、托盘退边、箱门余量、左右余量和顶部余量必须可配置。
- 多产品拼柜必须提供两种清单式输入：一是手工录入系列、产品代码、品名规格、产品数量、EA/BOX、不同外箱 L×W×H、包装方式及托盘尺寸；二是先选系列、再从该系列选择“产品代码 · 品名”，产品数量由本次装柜人员填写，其余主数据从本机产品库自动带出并锁定不可改。
- 多产品总箱数必须按 `ceil(产品数量 ÷ EA/BOX)` 计算，尾箱数量按 `产品数量 mod EA/BOX` 计算；超过单柜容量时自动完整分配至后续集装箱，不得静默遗漏任何可装纸箱。
- 拼柜为每个 SKU 建立可识别、可卸货的连续装载区，分区之间保留设定间隙；默认允许相邻 SKU 在地面未占用轮廓内交错补位，但任何实体包装绝不重叠或挤压，且可切换为严格矩形分区。分区内部必须搜索可执行的 0°/90° 混合落地排布，并把纵向外廓压缩到承载该 SKU 数量所需的最短值。每柜必须分别显示 BOX、实际 EA、托盘数、CBM、数量满足率、体积利用率、纵向利用率、剩余长度和各 SKU 分配结果。

## 2. 计算正确性（必须全部满足）

- 所有尺寸和余量以 mm 计算；纸箱尺寸余量加入长、宽、高，间隙只加在相邻包装单元之间。
- 有效箱内空间 = 内长减箱门余量、内宽减两侧余量、内高减顶部余量，不得出现负数。
- 任一可视化矩形不得越过其有效承载面的边界；任意两个包装单元不得重叠。
- 纸箱和组托后的单托包装必须按规定方向通过参考柜门开口；不能通过时装量必须为 0 并标注“禁止执行”。预设门洞为 20GP/40GP 2340 × 2292 mm、40HQ 2340 × 2597 mm，自定义柜型允许覆盖门宽门高。
- 纸箱直装数量 = 每层数量 × 高度层数。
- 托盘模式总箱数 = 柜底托位数 × 上下托盘层数 × 每托盘每层箱数 × 每托盘纸箱层数。
- 托盘上的纸箱不得越过退边后的有效托盘面；托盘总高不得超过用户限高或集装箱有效内高中的较小值。
- 托盘包装材积 = 实际托盘总数 × 托盘标称长 × 托盘标称宽 × 实际单托总高；纸箱直装包装材积 = 总箱数 × 纸箱标称外廓体积，统一换算为 CBM。尺寸公差仅用于可装性与间隙校验，不虚增对外报告的标称包装材积。
- 启用平底托盘双层后，两层实际总叠高不得超过集装箱有效净高；若双层未增加纸箱总数，必须选择托盘更少的单层方案。
- 算法必须比较长宽互换，并搜索两个主轴方向的混合分带方案；拼柜同一 SKU 分区允许 0°/90° 混合落地排布。先满足全部输入数量与最少分柜，再最小化各 SKU 分区纵向占用；不得用视觉拉伸掩盖实际空余。
- 任一输入变化必须立即重新计算并只呈现规则约束内的最优方案；先最大化包装单元数量，数量相同再最小化组合余隙。
- 纸箱体积利用率使用纸箱标称体积计算，且应与总箱数一致。
- 输入无效、空间不足或尺寸超限时数量为 0，并给出明确提示，不得出现 `NaN`、负数量或页面崩溃。
- 尾箱仍按完整外箱外廓占用一个装载位；产品数按实际尾数 EA 计，不得以 `箱数 × EA/BOX` 虚增。尾箱必须位于该 SKU 最后一个分区的最后装载位最上层，任何坐标仍需满足完整外箱的间隙和边界检查。
- 任一水平方向空隙合计超过 150 mm 时，页面和报告必须标明封柜前须补货或采用经批准的挡木、填充、支撑/系固措施，并要求签字复核；不得把大面积空余区仅标为“可追加货物”。
- 不同 SKU 交错或相邻边界必须使用颜色/代码标识，并按卸货要求配置瓦楞纸板、塑料片或纤维网等识别隔离材料；这些薄型隔板只承担识别和表面保护，不得被计作结构挡固。承载或填充较大空隙必须另行采用经批准的挡木、蜂窝板/承压板、空托盘、充气袋或系固方案。

## 3. 可视化一致性

- 必须提供俯视水平剖面、侧视纵向剖面和箱门端视横向剖面；托盘模式另提供单托盘排箱视图。
- 俯视图中正向、旋转 90°、预留空隙必须使用不同且有图例说明的视觉编码。
- 俯视图中的纸箱/托盘数量（未触发展示上限时）必须等于当前层计算数量。
- 侧视图层数、已用高度、剩余高度必须与摘要数字一致。
- 托盘模式必须同时标注柜底托位数、上下托盘层数、实际托盘总数、每托纸箱层数、单托总高、两层总叠高和柜内剩余高度。
- 托盘模式必须提供独立的“单托盘排箱”视图，并显示每层、层数、每托总箱数。
- 图面必须标注集装箱长度、宽度或高度，且在窄屏允许横向查看，不能压缩到不可读。
- 拼柜图必须按 SKU 使用稳定颜色和产品代码标识分区；尾箱以独立红色视觉编码标注实际 EA，柜门禁放区必须清晰可见。横向剖面必须按矩形实际覆盖的纵向区间求截面，不得仅按矩形起点分组造成漏画。

## 4. 交互与界面

- SAP 式严密逻辑与美集品牌视觉结合；正文不得小于 13 px、关键表格不得小于 12 px、主操作触控高度不得小于 40 px，兼顾清晰层级、专业蓝灰配色和适度信息密度。
- 桌面端和手机端均可完成所有输入、模式切换和视图切换；键盘可聚焦所有表单与按钮。
- 手机端宽度 320–430 px 时不得出现页面级横向溢出；输入和主按钮触控高度不小于 44 px，剖面图在局部容器内横向滑动查看。
- 软件页面底部必须显示浙江美集实业有限公司、版权年份、MEGEE COSPACK、软件版本号和与发布源码一致的 Git 短提交号（Build）；正式报告页脚必须显示相同版本号和报告编号，便于追溯。
- 修改任一输入后，统计数字、摘要和可视化必须同步更新。
- “恢复默认”还原默认尺寸和标准余量；“打印方案”可生成无控制面板干扰的打印版。
- “输出报告”必须生成正式打印版，包含报告编号/日期、全部参数、EA、三柜型对比、最优方案、多剖面图、摆放原则、风险说明和签字栏，并可由浏览器存为 PDF。
- 正式报告必须可在“中文版”和“English”之间选择；英文版必须完整翻译字段、操作步骤、图名、图内方向标注、复核原则和签字栏，不能只翻译标题。
- 正式报告必须包含外箱 L × W × H 尺寸示意图，明确箱高向上和底面仅可旋转 90°；不得只用一行尺寸文字替代现场可识别图示。
- 正式报告必须把算法结果转为操作指令：从箱头向箱门的装载方向、图面编号顺序、每层 0°/90° 数量、总层数，以及托盘模式的每托每层数量、纸箱层数和上下托盘层数。
- 正式报告的俯视、纵向剖面、箱门横向剖面必须与装量结论一致；托盘模式另附单托排箱图。操作人员仅凭报告即可识别产品、包装尺寸、选用柜型、装箱数量和具体摆放方式。
- 托盘方案未达到客户高度或最低柜容利用率时，报告必须显著标注“不推荐执行”，不得把分析方案表述为正式执行方案。
- 报告跨页表格允许自然续页，续页必须重复表头；报告正文不得小于 8.5 pt、关键数据不得小于 10 pt、图内操作编号不得小于 5.5 pt；章节标题、图示和签字栏不得孤立、裁切或重叠。
- 报告不得产生空白页、仅有页眉页脚的页面或明显不足半页的孤立尾页；装载图、横向剖面和外箱示意必须整体分页，不得被分页裁切。
- 正式报告必须显示包装总材积 CBM。
- 拼柜输入表、分柜明细和正式报告必须逐 SKU 显示 CBM：纸箱按总箱数（含尾箱完整箱位）×外箱外廓计算；托盘按托盘数×托盘长宽×实际组托总高计算；逐 SKU、逐柜和整单合计必须一致。
- 打印按钮必须在运行时自检通过后才能启用；至少核对需求与分柜的 BOX、EA、CBM 合计、分柜小计、三轴边界、装载高度、单元重叠和规定间隙。点击输出时必须再次核对报告中产品表、包装表、逐柜装载图、横向剖面和分配表的数量及结构，发现缺图、缺行或无效数值时禁止调用打印。
- 装柜算法和 Excel 解析只在浏览器内完成；导入数据仅写入当前浏览器本地存储。
- 拼柜正式报告必须逐柜分页，列出每柜多少 BOX、多少实际 EA、每个 SKU 的外箱尺寸/分区/层数/尾箱，并附装柜顺序、柜门禁放区和现场签字栏。
- 尾箱操作说明必须要求：内部空隙使用合规缓冲材料填实、单独封箱并标注实际 EA、区末最上层放置、禁止挤压变形或在其上堆放满箱；更换小规格尾箱必须作为独立尺寸重新计算。

## 5. 工程与发布

- 生产构建无 TypeScript/打包错误。
- 自动化算法测试全部通过，至少覆盖：混合方向优于单一方向、间隙改变数量、越界/重叠检查、超大包装单元返回 0、标准托盘组合、40HQ 双层增加数量、20GP/40GP 数量相同时回退单层、禁用双层。
- 多产品算法测试必须覆盖不同外箱、同一 SKU 0°/90° 混排、间隙与三轴越界、跨多柜不丢箱、实际 EA 不重复、尾箱完整占位、尾箱最上层标识、纸箱/托盘混装及超尺寸纸箱/托盘明确报错。
- 多产品算法测试必须同时覆盖 SKU 严格分区与边界交错模式，证明交错能够减少纵向占用或减少分柜，并逐对验证包装单元无重叠、SKU 间隙不缩水、总 BOX/EA/CBM 不变。
- 发布前必须使用真实 Chromium 生成中英文 A4 PDF 并逐页渲染复核；另以不少于 12 个 SKU 的跨页样本确认续页重复表头、图形整体分页、无空白页和无孤立尾页。
- GitHub 仓库源码与 Cloudflare 发布所用提交必须一致。
- 本地地址、Cloudflare 线上地址均能正常打开并完成两种模式计算。
- 正式线上地址必须位于 Cloudflare Workers、自有域名或 Pages，不得使用 `chatgpt.site`。
- 公开部署不得包含产品快照、Cost 凭据、成本、BOM、客户或其他保密字段；产品库只能由当前用户在本机导入或手工建立。
- Excel 导入必须幂等、可追踪；失败时保留最后一次成功导入的浏览器缓存并显示错误，不得把失败显示为成功。

## 6. 产品导入与报告治理

- 产品代码是导入唯一键；同一 Excel 内重复产品代码必须拒绝导入，重复导入不得生成重复 SKU。
- EA/BOX 允许为空，不得默认填充；美集外箱尺寸缺失时自动补为 480 × 380 × 350 mm，托盘包装尺寸缺失时记录必须标记“待补齐”。
- 每次导入必须重新计算全量自动方案，同时保留相同产品代码的人工备注与复核状态。
- 报告必须包含产品数据导入时间、装柜算法版本、方案版本、生成日期和复核状态。
- 客户可使用公开软件自行导入标准模板；公开页面不得自动连接内部 Cost 系统。
- 应用不得生成携带产品或方案参数的分享链接；对外交付仅允许使用经人工复核的正式 PDF。内部产品库如需集中管控，必须部署在受身份验证保护的独立入口。

## 7. 明确边界

首版输出为规则化工程预估，不替代现场复核。当前不计算轴载、总载重、纸箱抗压、装卸顺序、门框/角柱局部障碍和复杂自由形状拼箱；页面必须持续显示此限制。
