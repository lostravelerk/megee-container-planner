# 集装箱装柜规划 v3.0.0 检验与验收标准

本标准是构建、保存、输出和发布的强制门槛。任何核心项未通过，系统必须明确阻止“确认方案”和正式报告输出，不得以警告代替失败。

## 1. 产品边界

- 系统只保留两个工作区：`装柜规划`与`装柜方案库`。
- 方案库只保存用户实际计算并明确保存的方案快照；首次打开必须为空。
- 不内置企业产品库，不连接 Cost，不提供 Excel 批量预规划，不保存成本、BOM、客户或其他敏感资料。
- 不保留单品装柜模块；单 SKU 与多 SKU 均使用同一张紧凑产品清单。
- 品牌沿用 `MEGEE / MEGEE COSPACK` 名称，采用克制的纯文字标识，不使用低质量图片 Logo；信息层级、操作效率和报告可读性优先于装饰。

## 2. 输入与规划模式

- 产品行必须在同一行提供：系列、产品代码、品名规格、产品数量、数量规则、EA/BOX、外箱 L×W×H、包装方式、托盘参数、箱数、CBM、尾箱数量。
- 包装方式默认纸箱，可逐行改为托盘；托盘行必须填写实际托盘 L×W×H 和允许外伸。
- 外箱默认 480×380×350 mm；托盘默认 1000×1200×150 mm；EA/BOX 与产品数量不得提供虚构默认值。
- 数字输入必须可清空后重新输入，不能因为最小值校验把用户锁在旧数字；失焦或提交时再执行边界校验。
- 订单数量模式严格按输入订单量规划。箱数 = `ceil(产品数量 / EA/BOX)`，尾箱数量 = `产品数量 mod EA/BOX`；产品数量不要求为 EA/BOX 的倍数。
- 柜容反算模式支持 1–20 个 20GP、40GP 或 40HQ；逐行数量规则可选固定、可调、齐套。
- 固定数量不得被算法修改；可调数量必须落在用户 Min/Max 内并尽量接近 Target；相同齐套组代码的 SKU 最终 EA 必须一致。
- 柜容反算中的齐套约束只约束组件 EA 相等，不要求整箱倍数；尾箱仍占一个完整受保护箱位。
- 用户未填写范围时，系统必须基于实际柜容推导有界搜索范围，禁止无界暴力枚举。

## 3. 装柜算法硬约束

- 纸箱高度必须向上，底面只允许 0°/90° 旋转；禁止侧放、倒置、挤压、重叠或越界。
- 全部尺寸和余量以 mm 计算。有效空间 = 内尺寸减门口、两侧和顶部余量；相邻包装单元之间按设置保留间隙。
- 每个候选方案必须经过真实三维坐标审计：数量守恒、边界、门洞、层高、重叠、相邻间距和托盘外廓全部通过。
- 算法必须比较主轴方向和 0°/90° 混排；在满足全部订单量前提下先最少分柜，再提高实际装载率并压缩纵向占用。
- 不允许用 CBM 理论值代替几何可装性。理论 CBM 可行但实际排布失败的候选必须淘汰。
- 20GP、40GP、40HQ 使用已定义的内尺寸和柜门尺寸；40HC 不作为独立柜型，统一为 40HQ。
- 尾箱按完整外箱外廓占位，标明实际 EA，并置于该 SKU 最后分区的最上层；不得在尾箱上继续承重。
- 任一 SKU 未规划完毕、任一柜超出指定柜数、任何数值为 NaN/Infinity/负数或审计失败，方案均不可确认或输出。

## 4. 托盘算法硬约束

- 托盘上的纸箱同样高度向上并允许底面 90° 旋转；必须显示每层排法、纸箱层数、每托总箱数和货载外廓。
- 默认托盘 1000×1200×150 mm、允许外伸 0 mm/边、客户总高范围 1200–1800 mm，并默认允许平底托盘双层。
- 实际装柜占位必须使用 `max(托盘原始外廓, 纸箱货载外廓) + 包膜/歪斜余量`，不能只按标准托盘尺寸占位。
- 纸箱只能落在托盘允许边界内；只有用户明确输入允许外伸时才可越过原始托盘边缘。
- 托盘之间、纸箱之间、托盘安全退边和包膜/歪斜余量必须分别计算，不得重复计算或遗漏。
- 双层托盘的下层顶面必须平整、完整且可承托上层；不满层托盘只能放在最上层，并标明“补平前禁止上叠”。
- 单托总高不得超过客户上限；双层总高不得超过集装箱有效净高和柜门高度。
- 纸箱、托盘混装时必须保持各 SKU 数量守恒、独立外廓和可执行的装卸顺序。

## 5. 结果与可视化

- 每柜必须显示：SKU、EA、CTN、PLT、CBM、体积利用率、纵向利用率、剩余长度和尾箱。
- 每柜必须提供俯视图、纵向侧视图、箱门端视图；存在托盘 SKU 时另提供逐 SKU 单托盘排箱图。
- 图面必须标注箱头、箱门、装载方向、内长宽高、SKU 代码、0°/90°方向、层数、尾箱及禁放/余量区域。
- 图形按真实比例呈现；不能通过拉伸、合并或省略隐藏实际空余。
- 不同 SKU 使用稳定且可读的颜色与代码双重编码；颜色不是唯一识别手段。
- 页面只呈现可执行的最终方案和必要风险，不显示无法指导操作的内部搜索碎片。

## 6. 方案库与版本治理

- 方案必须区分草稿和已确认状态，记录方案号、修订号、创建时间、更新时间、柜型、柜数和汇总。
- 保存后必须冻结完整输入参数、数量规则、算法配置和结果摘要；打开历史方案不得被其他数据静默覆盖。
- 方案库采用紧凑固定表头，列头提供筛选漏斗，支持检索、排序、打开、HTML 报告和删除。
- 删除必须二次确认；全部业务数据默认只保存在当前浏览器本地存储。
- 页脚和正式报告必须显示应用版本、算法版本、源码 Git 短提交号和报告编号。

## 7. HTML 报告、PDF 与 Packing List

- HTML 报告是主报告视图，与打印/PDF 使用同一数据和 DOM，不允许维护两套相互漂移的结果。
- 正式装柜报告必须包含：公司、方案与报告编号、生成日期、柜型/柜数、产品信息、包装参数、逐柜汇总、分柜明细、三视图、托盘图（如有）、装柜顺序、尾箱/固定措施和签字栏。
- Packing List 必须独立输出，包含客户/订单留白、系列、代码、品名规格、包装方式、EA、EA/BOX、CTN、PLT、外箱、尾箱、逐 SKU/逐柜/整单 CBM 和合计。
- 中文与英文必须是完整双语切换；标题、列名、指引、图内标注、风险、签字栏和页脚均须翻译。
- 报告输出前必须再次比较报告行数、逐柜分区数、EA/CTN/PLT/CBM 合计和算法结果；不一致时阻止打印。
- 表格跨页必须重复表头，表格行不得被拆分；图、标题、风险框、签字栏不得跨页或被裁切。
- 不允许空白页、仅页眉页脚页面、明显不足半页的孤立尾页、文字重叠、过小字号或横向截断。
- A4 PDF 正文不小于 8.5 pt，关键数值不小于 10 pt；图内关键操作文字必须保持可辨认。

## 8. 界面与响应式

- 桌面端采用 SAP 式紧凑列表、固定表头、清晰网格和中性专业配色；避免大面积装饰卡片、冗余说明和无效留白。
- 桌面宽屏应尽量一次看到所有核心列；尺寸使用紧凑 L/W/H 分格输入。
- 320–430 px 手机端必须能完成新建、录入、切换模式、查看结果、保存和输出；页面主体不得整体横向溢出。
- 手机端表格可转换为清晰行卡片；大型比例图只允许在自身容器内横向查看。
- 所有主按钮触控高度不小于 40 px，移动端不小于 44 px；键盘焦点和表单标签必须完整。

## 9. 自动化发布门槛

- `npm run lint`、生产构建和全部自动化测试必须通过。
- 测试必须覆盖：不同外箱、0°/90°混排、间隙变化、越界/重叠、跨多柜不丢箱、尾箱、纸箱/托盘混装、托盘外伸、双层托盘、固定/可调/齐套数量和报告结构。
- 必须保留 404/24 牙喷头与外罩 20GP 等量齐套回归样本：144,000 套，泵头 144 CTN，外罩 229 CTN，合计 373 CTN，23.778144 CBM。
- 发布前必须在真实 Chromium 完成桌面与手机操作回归，验证数字可清空修改、包装方式可切换、保存/筛选/打开、HTML 报告和打印调用。
- 发布前必须生成真实 PDF，逐页转成图片检查分页、字体、表头重复、图形完整、合计和无空白页。
- GitHub 主分支提交、页面底部 Build 号和 Cloudflare 线上部署必须来自同一源码提交。
- 正式地址使用自有域名，不得使用 `chatgpt.site`；静态前端和算法可迁移到企业内网或其他云环境。

## 10. 使用边界

系统输出是经过规则与几何审计的装柜执行方案，但不能替代装柜前实测和现场安全复核。纸箱抗压、实际重量、轴载、危险品、柜内结构偏差、受潮、运输动态载荷和承运人特殊要求，必须由包装、物流和现场负责人另行确认。
